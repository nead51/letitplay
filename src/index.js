import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import LIP from './LIP';

ReactDOM.render(
  <React.StrictMode>
    <App />
    <LIP />
  </React.StrictMode>,
  document.getElementById('root')
);
